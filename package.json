{
  "name": "opencode-kilo-gateway",
  "version": "0.1.0",
  "description": "Kilo Gateway provider plugin for OpenCode",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "sync:kilo": "node ./scripts/sync-kilo-gateway.mjs"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^0.15.31",
    "open": "^10.1.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
